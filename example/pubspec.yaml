name: backendless_sdk_example
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_messaging: ^11.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  backendless_sdk:
    path: ../


flutter:
  uses-material-design: true